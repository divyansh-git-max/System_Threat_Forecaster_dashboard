[project]
name = "system-threat-forecaster"
version = "1.0.0"
description = "React and FastAPI ML dashboard for system threat forecasting."
requires-python = ">=3.12,<3.15"
dependencies = [
  "fastapi>=0.117.1",
  "uvicorn[standard]>=0.35.0",
  "python-multipart>=0.0.20",
]
